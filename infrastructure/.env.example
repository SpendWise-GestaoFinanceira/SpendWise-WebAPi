# ========================================
# SpendWise Backend - Environment Variables
# ========================================
# IMPORTANTE: Copie este arquivo para .env e preencha com seus valores reais
# O arquivo .env NAO deve ser commitado no Git

# ========================================
# Database Configuration
# ========================================
POSTGRES_DB=spendwise
POSTGRES_USER=spendwise
POSTGRES_PASSWORD=spendwise123
POSTGRES_PORT=5432

# ========================================
# API Configuration
# ========================================
API_PORT=5000
ASPNETCORE_ENVIRONMENT=Development

# ========================================
# JWT Authentication
# ========================================
# IMPORTANTE: Use uma chave forte com pelo menos 32 caracteres
# Exemplo de geracao: openssl rand -base64 32
JWT_SECRET_KEY=sua_chave_secreta_aqui_deve_ter_pelo_menos_32_caracteres_muito_segura
JWT_ISSUER=SpendWise
JWT_AUDIENCE=SpendWise-Users

# ========================================
# Email Service (Brevo)
# ========================================
# Provider: Mock (desenvolvimento) ou Brevo (producao)
EMAIL_PROVIDER=Mock
# API Key do Brevo (obtenha em: https://app.brevo.com/settings/keys/api)
EMAIL_API_KEY=sua_api_key_do_brevo_aqui
EMAIL_SENDER_EMAIL=noreply@spendwise.com
EMAIL_SENDER_NAME=SpendWise

# ========================================
# Adminer Configuration
# ========================================
ADMINER_PORT=8080

# ========================================
# Notas de Desenvolvimento
# ========================================
# 1. Para desenvolvimento local, use EMAIL_PROVIDER=Mock
#    Isso ira logar emails no console ao inves de enviar
#
# 2. Para testar envio real de emails, use EMAIL_PROVIDER=Brevo
#    e configure sua API Key
#
# 3. Nunca commite o arquivo .env com credenciais reais
#
# 4. Para gerar uma JWT Secret Key segura:
#    - Linux/Mac: openssl rand -base64 32
#    - PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
#
# 5. Portas padrao:
#    - API: 5000
#    - PostgreSQL: 5432
#    - Adminer: 8080
